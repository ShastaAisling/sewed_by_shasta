<div class="container" style="margin: 5%">
  <h1 class="display-3">Past Projects</h1>
  <p class="lead">A collection of some favourite past projects!</p>
  <hr class="my-4">
  <div>
    <% @past_projects.each do |past_project| %>
      <%= render partial: "shared/past_project_preview_card", locals: { past_project: past_project } %>
    <% end %>
  </div>
</div>
